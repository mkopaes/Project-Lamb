-- Database used: lambda_database

CREATE TABLE registered_people (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL,
    birth_date DATE,
    gender VARCHAR(50),
    ethnicity VARCHAR(50),
    profession VARCHAR(100)
);